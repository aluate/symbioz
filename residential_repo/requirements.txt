# ACC Express Order - Python Dependencies
# Install with: pip install -r requirements.txt

# FastAPI web framework
fastapi>=0.104.0

# ASGI server
uvicorn[standard]>=0.24.0

# Data validation
pydantic>=2.0.0

# Form data parsing (if needed for file uploads)
python-multipart>=0.0.6

# Optional: For running tests (TestClient)
# httpx>=0.25.0
