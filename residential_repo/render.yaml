services:
  - type: web
    name: residential-trim-app
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn apps.web.trim_api:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true

