storage:
  default_repo_root: "."      # repository root
  reports_dir: "reports"

safety:
  auto_apply_repairs: false
  auto_refactor_repo: false

# LLM configuration (to be added in Phase 2)
# llm:
#   provider: "openai"  # or "anthropic", "ollama", etc.
#   model: "gpt-4"
#   api_key: "${OTTO_LLM_API_KEY}"

# Life OS integration (to be added in Phase 2)
# life_os:
#   api_url: "http://localhost:8000"
#   api_key: "${LIFE_OS_API_KEY}"

